# =============================================================================
# AWS Lambda Provider - Apenas Serverless
# =============================================================================
apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-aws-lambda
  namespace: crossplane-system
  labels:
    app.kubernetes.io/name: aws-lambda-provider
    app.kubernetes.io/component: crossplane-provider
    app.kubernetes.io/part-of: crossplane
    app.kubernetes.io/managed-by: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  package: xpkg.upbound.io/upbound/provider-aws-lambda:v1.0.0
  packagePullPolicy: IfNotPresent
  ignoreCrossplaneConstraints: false
